import styled from "styled-components";

const StyledChangeThemeButton = styled.button`
  all: unset;

  display: flex;
  align-items: center;
  font-size: 1.4rem;
  cursor: pointer;
  transition: all 0.4s ease-out;

  svg {
    margin-right: 0.5rem;
  }
`;

export { StyledChangeThemeButton };
